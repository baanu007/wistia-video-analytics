# Screenshots

Drop dashboard screenshots here and reference them from the main README:

- `dashboard_overview.png` — Streamlit / QuickSight homepage showing video performance KPIs (views, watch time, engagement rate)
- `engagement_breakdown.png` — drill-down chart for top videos by engagement segment
- `workflow_graph.png` — Glue Workflow visualization showing the ingest -> transform -> dq_load chain
- `redshift_query.png` — sample analytics SQL run against the curated Redshift schema

Recommended source: take screenshots from your QuickSight dashboard (after running the pipeline against a Wistia test workspace) or the local Streamlit app under `dashboard/`.
