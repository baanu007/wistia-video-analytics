aws_region       = "us-east-1"
aws_profile      = "globalpartners"
environment      = "prod"

# Wistia API Bearer token (rotate if exposed)
wistia_api_token = "REPLACE_ME"

# Media IDs to track
media_ids        = ["gskhw4w4lm", "v08dlrgr7v"]

# Email that receives SNS alerts when the pipeline fails
alert_email      = "you@example.com"
